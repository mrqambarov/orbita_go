import 'package:flutter/material.dart';

class TariffSelector extends StatelessWidget {
  const TariffSelector({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
